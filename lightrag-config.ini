[DEFAULT]
# LightRAG Configuration File
# Most settings are configured via environment variables
# This file can override environment settings if needed

[server]
# Server settings (overridden by environment variables)
host = 0.0.0.0
port = 9621

[logging]
# Logging configuration
level = INFO
format = %(asctime)s - %(name)s - %(levelname)s - %(message)s

[security]
# Security settings
# Enable authentication if needed (configure in environment)
# auth_required = false 